<%# for each site in @sites: generate link (#show) and fill it with partial _site_content.html.erb %>
<%= link_to render(:partial => 'sites/site_content', locals: {site: site}), site, remote: true if can? :show, Site %>